# Cloud / default install — keeps Streamlit Community Cloud light & fast to wake.
# Theme discovery uses TF-IDF fallback when BERTopic is not installed.
streamlit>=1.32.0
pandas>=2.1.0
numpy>=1.26.0
plotly>=5.18.0
vaderSentiment>=3.3.2
scikit-learn>=1.4.0
openpyxl>=3.1.0
langdetect>=1.0.9
snownlp>=0.12.3
pyarrow>=14.0.0
