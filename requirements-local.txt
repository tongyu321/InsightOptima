# Full local NLP stack (optional). Install on your PC when you want BERTopic:
#   pip install -r requirements.txt -r requirements-local.txt
#
# Not used by Streamlit Community Cloud (that only reads requirements.txt).
datasets>=2.18.0
bertopic>=0.16.0
sentence-transformers>=2.2.0
umap-learn>=0.5.0
hdbscan>=0.8.0
transformers>=4.36.0
torch>=2.0.0
